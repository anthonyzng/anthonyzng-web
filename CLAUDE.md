# CLAUDE.md

Guidance for Claude Code (and any sub-agent) working in this repository.

> Items marked `TBD` are open decisions. Do not guess them; ask the owner.

---

## 1. Project Overview

**anthonyzng-web** is the personal professional showcase website of Anthony Ng, served at **owwsolution.com**.
GitHub repo: **`anthonyzng/anthonyzng-web`** (public).

The site must communicate the owner's three roles:
- Full Stack Software Developer
- AI Developer
- Assistant Manager, Software Development

### Features
- Work experience timeline
- Projects portfolio (details, tech stack, links, images)
- Skills / tech stack
- Contact form (with bot protection)
- Downloadable CV (PDF)
- **Bilingual: English + Traditional Chinese** (language switcher, content stored per locale)
- **Dark / light mode** (follows system by default, user toggle persisted)
- SEO (meta tags, Open Graph, sitemap, `hreflang`, structured data)
- Analytics
- **Admin panel** to manage experience, projects, skills, and CV content (content lives in the database)

### Home page requirements (non-negotiable)
- **Scroll-driven animations**: elements animate in sync with scroll progress, not just a one-time fade-in.
- **Parallax scrolling**: layered elements moving at different speeds.
- Smooth (target 60fps), honours `prefers-reduced-motion`, degrades gracefully on mobile and older browsers.

### Design direction
Minimal, modern, professional. Reference style: editorial portfolios such as those collected on Awwwards and a1.gallery.
- Generous whitespace, strong typography (grotesk sans such as Inter / Neue Montreal, plus a mono accent such as IBM Plex Mono)
- Restrained palette: neutral base plus one accent colour
- Motion is tasteful: layered parallax on individual elements, text reveals tied to scroll, pinned sections for storytelling (e.g. the experience timeline)
- Use the `ui-ux-pro-max` skill for design system decisions

---

## 2. Architecture

Each tier runs in its **own Docker container** so they can be scaled or moved independently.

```
                       Internet
                          │
          owwsolution.com │ api.owwsolution.com
                ┌─────────▼─────────┐
                │   proxy (Caddy)    │  auto-HTTPS, routing by host
                └───┬───────────┬───┘
                    │           │
        ┌───────────▼──┐   ┌────▼──────────┐
        │  frontend     │   │  backend       │
        │  React SPA    │   │  FastAPI       │
        │  (static)     │   │                │
        └──────────────┘   └────┬──────────┘
                                │
                          ┌─────▼──────┐
                          │ PostgreSQL  │  named volume
                          └────────────┘
```

- Hosting: **GCP Compute Engine VM** running Docker + Docker Compose. **Every service (frontend, backend, database, proxy, and any future service) runs in its own container**; nothing is installed directly on the host besides Docker.
- Domains: `owwsolution.com` + `www` (redirects to the apex) → frontend; `api.owwsolution.com` → backend. DNS: Cloudflare (nameservers `adele.ns.cloudflare.com`, `owen.ns.cloudflare.com`, registrar Namecheap), the three A records proxied (orange cloud), SSL/TLS mode Full (strict): Cloudflare in front gives DDoS protection, the CDN and Web Analytics; Caddy still holds its own Let's Encrypt certificates (section 6.3, 7).
- Database backups: not required for now.

### Tech stack

| Layer      | Choice |
|------------|--------|
| Frontend   | React + TypeScript + Vite |
| Styling    | Tailwind CSS |
| Animation  | GSAP + ScrollTrigger (scroll-driven, pinning, parallax), Lenis (smooth scroll) |
| i18n       | react-i18next; locales `en`, `zh-Hant` |
| Routing    | React Router |
| Backend    | Python 3.13 + FastAPI, SQLAlchemy 2.x (async, asyncpg), Alembic migrations, Pydantic v2; dependencies managed with uv (`pyproject.toml` + committed `uv.lock`) |
| Database   | PostgreSQL 17; bilingual content stored as one JSONB `translations` column per row (see section 5) |
| Proxy/TLS  | Caddy (automatic Let's Encrypt certificates) |
| CI/CD      | GitHub Actions: lint/test → build images → push to GHCR → deploy to GCP VM over SSH |
| Admin auth | Single admin account, Argon2 password hash, JWT in httpOnly cookie, login rate limiting (TOTP 2FA later). Admin UI at `owwsolution.com/admin` |
| Contact    | Cloudflare Turnstile (bot protection) + Resend (email from `noreply@owwsolution.com`). Every message is also stored in the DB. Recipient comes from env var `CONTACT_TO_EMAIL` (value kept in `CLAUDE.local.md`, never committed) |
| Analytics  | Cloudflare Web Analytics (owner decision, Phase 8: no cookie, no container on the 1 GiB VM; enabled in the Cloudflare dashboard with automatic setup, which injects the beacon at the edge, allowed by the CSP) |

---

## 3. Repository Layout

```
anthonyzng-web/
├── CLAUDE.md
├── README.md
├── docker-compose.yml          # the full stack: db, migrate (one-off), backend, frontend, proxy
├── docker-compose.local.yml    # override: the full stack on this machine over HTTP (localhost:8080)
├── docker-compose.dev.yml      # the `db` service alone (PostgreSQL 17 on 127.0.0.1:5432) for host-run dev
├── .env.example                # root Compose variables (Postgres credentials, addresses, frontend build args), placeholders only
├── .mcp.json                   # GitHub MCP server (token read from env var GITHUB_PAT)
├── .claude/
│   ├── settings.json           # project plugins
│   └── commands/do_pr.md       # /do_pr command
├── db/
│   └── init/                   # Postgres init scripts (creates the `*_test` database on first start)
├── frontend/
│   ├── Dockerfile              # multi-stage: npm build → unprivileged nginx on :8080
│   ├── nginx.conf              # SPA fallback, asset caching, /healthz
│   ├── .env.example            # VITE_API_BASE_URL, VITE_TURNSTILE_SITE_KEY (public build-time values)
│   ├── scripts/                # sync-content.mjs (`npm run content:sync`)
│   └── src/
│       ├── api/                # fetch client, zod schemas, /content and /contact calls, file URLs
│       ├── components/         # incl. ContactForm, FormField, ProjectCard
│       ├── content/            # resolved shape, snapshot/<locale>.json (saved API payload), useContent + ContentProvider
│       ├── sections/           # Hero, Experience, Projects, Skills, Contact
│       ├── animations/         # scroll / parallax hooks
│       ├── i18n/               # UI string translations, formatMonth, formatFileSize
│       ├── admin/              # admin panel (Phase 5)
│       └── pages/
├── backend/
│   ├── Dockerfile              # multi-stage: uv sync → slim runtime, non-root, uvicorn on :8000
│   ├── pyproject.toml          # uv-managed project; uv.lock is committed
│   ├── .env.example            # every backend env var, placeholder values only
│   ├── alembic.ini
│   ├── alembic/                # async env.py + versions/
│   ├── tests/                  # pytest against a real PostgreSQL (TEST_DATABASE_URL)
│   └── app/
│       ├── main.py             # create_app() + lifespan; `app` for uvicorn (built on first access)
│       ├── api/                # deps.py + v1/ routers (health, content, contact, auth, files, admin)
│       ├── models/             # SQLAlchemy models (content, contact_message, admin_user, stored_file)
│       ├── schemas/            # Pydantic schemas (public payload, write models, admin, requests, errors)
│       ├── services/           # content, content_admin, collections, files, messages, contact, turnstile, email, auth
│       ├── seed/               # content.json + `python -m app.seed` (bootstraps an empty database)
│       └── core/               # config, db, security, rate_limit, client_ip, errors, middleware
├── proxy/
│   ├── Caddyfile               # hosts from env, TLS, frontend security headers, body cap
│   └── local/                  # local-only routes (mounted by docker-compose.local.yml)
├── deploy/
│   └── vm-setup.sh             # one-time VM setup (Docker, swap, deploy user, stack directory)
└── .github/workflows/
    └── ci-cd.yml               # CI on every PR / push; images to GHCR and deploy on main
```

---

## 4. Development Workflow (MUST follow)

### 4.1 Ask first
For every request from the owner: if anything is unclear, ambiguous, or missing detail, **ask before executing**.

### 4.2 Per-task flow

```
Owner assigns a task
   → clarify if needed
   → check the current branch
        • on main      → create a new branch: ddmmyyyy_hhmmss_by_claude_anthony
        • not on main  → keep working on the current branch
   → implement
   → code review the diff (code-review skill); fix every finding and re-review until clean
   → commit
   → push to the current branch
```

- Branch name uses local time at creation, e.g. `21092026_143005_by_claude_anthony`.
  PowerShell: `git checkout -b "$(Get-Date -Format 'ddMMyyyy_HHmmss')_by_claude_anthony"`
- **Never commit directly to `main`** (the one exception is the initial repository commit).
- The same branch keeps receiving commits and pushes across tasks until the owner runs **`/do_pr`**.
- `/do_pr` (see `.claude/commands/do_pr.md`): opens a PR from the current branch into `main` via the GitHub MCP server, squash-merges it, and syncs local `main`. The next task then starts a new branch.
- Merges into `main` trigger automatic deployment to the GCP VM.
- Commit messages: Conventional Commits (`feat:`, `fix:`, `chore:`, `docs:`, `refactor:`, `style:`, `test:`).
- GitHub operations (PRs, merges, repo metadata) go through the **GitHub MCP server**. Plain `git` over SSH is used for commit, push, and pull.

### 4.3 GitHub account
- Use **only** the GitHub account `anthonyzng`. Never use `API-Anthony-Ng`.
- SSH uses `~/.ssh/id_ed25519_anthonyzng` (already configured in `~/.ssh/config`).
- GitHub MCP (`.mcp.json`) authenticates with a fine-grained PAT scoped to this repo only, read from the Windows user env var `GITHUB_PAT`. Never print, log, or commit its value. When it expires (90 days), ask the owner to create a new one.

### 4.4 Security
- **Before creating, using, or storing any key, token, password, or credential, ask the owner for authorisation.** This covers GitHub tokens, GCP service accounts, SSH deploy keys, GitHub Actions secrets, DNS changes, firewall rules, and API keys.
- **Any task that involves personal data (emails, phone numbers, addresses, ID numbers, CV details, contact-form submissions, etc.) requires the owner's approval before acting**, including reading, storing, moving, publishing, or committing it.
- This repo is **public**. This file is committed, so never put private details in it; use `CLAUDE.local.md` (git-ignored) for those.
- Never commit secrets. They live in `.env` (git-ignored) or GitHub Actions secrets; keep `.env.example` in sync with placeholders.

---

## 5. Coding Conventions

### General
- Code, comments, commit messages, and docs are written in **English**.
- User-facing content is available in both `en` and `zh-Hant`. Never hard-code UI strings; use i18n keys.

### Frontend
- TypeScript strict mode. Function components + hooks, one component per file, PascalCase filenames.
- Animation logic lives in `src/animations/` as reusable hooks (`useParallax`, `useScrollReveal`, ...). Sections consume hooks rather than wiring GSAP inline.
- Use `gsap.context()` / `useGSAP` and always clean up ScrollTriggers on unmount.
- Always honour `prefers-reduced-motion`.
- Animate only `transform` and `opacity`.
- Mobile-first; verify at 375px, 768px, 1280px+ (use the `webapp-testing` skill).
- Accessibility: semantic HTML, alt text, keyboard navigation, WCAG AA contrast in both themes.

### Backend
- Type hints everywhere (`mypy --strict` must pass); Pydantic schemas for all request/response bodies, camelCase on the wire (`CamelModel` in `app/schemas/common.py`), snake_case in Python.
- All schema changes go through Alembic migrations (`alembic revision --autogenerate`); `tests/test_migrations.py` fails when the models and the migrations drift apart.
- Config comes from environment variables only (pydantic-settings, `app/core/config.py`); secrets are `SecretStr` and never logged.
- Localised content storage model: every translatable row carries one JSONB column `translations` shaped `{"en": {...}, "zh-Hant": {...}}`; both locales are required and validated by the Pydantic generic `Localized[T]`. Tag chips are a JSONB array whose items are either a plain string (a proper noun, never translated) or a `{"en": "...", "zh-Hant": "..."}` object (a translated term). Untranslated facts (company, school, certification name, href, display) are ordinary columns. Dates are `YYYY-MM` strings (`end_month` NULL = present). Slugs (`^[a-z][a-zA-Z0-9_-]{0,63}$`) are the frontend's ids and the natural key of every content table.
- Async everywhere (SQLAlchemy async sessions, asyncpg, httpx.AsyncClient); `ruff check` and `ruff format --check` must pass; tests run against a real PostgreSQL, never SQLite.

---

## 6. Commands

```bash
# Frontend (run inside frontend/)
npm install
npm run dev          # http://localhost:5173 (also the "frontend" entry in .claude/launch.json)
npm run typecheck    # tsc -b
npm run lint         # oxlint
npm test             # vitest (jsdom + Testing Library)
npm run build        # typecheck + production build to dist/
npm run preview      # serve dist/ on http://localhost:4173 (the "frontend-preview" entry in .claude/launch.json; for Lighthouse runs against a production build)
npm run content:sync # save GET /content (both locales) as src/content/snapshot/*.json; default http://localhost:8000, or `-- <api origin>`

# Dev database (from the repo root; credentials in the git-ignored root .env, see .env.example)
docker compose -f docker-compose.dev.yml up -d db   # PostgreSQL 17 on 127.0.0.1:5432, plus the *_test database

# Backend (run inside backend/; uv is invoked as `python -m uv`, plain `uv` may not be on PATH)
python -m uv sync --all-groups                 # create .venv from uv.lock (runtime + dev tools)
python -m uv run alembic upgrade head          # apply migrations to DATABASE_URL (never run at startup)
python -m uv run python -m app.seed            # load app/seed/content.json into an empty DATABASE_URL (refuses one with content; --force overwrites)
python -m uv run uvicorn app.main:app --reload --port 8000   # http://localhost:8000/api/v1, docs at /api/v1/docs (the "backend" entry in .claude/launch.json runs it too)
python -m uv run ruff check .                  # lint
python -m uv run ruff format --check .         # formatting
python -m uv run mypy .                        # strict type check
python -m uv run pytest -q                     # tests against TEST_DATABASE_URL (schema rebuilt by the session fixture)
python -m uv run alembic revision --autogenerate -m "describe change"   # after a model change

# Full stack (from the repo root; needs .env and backend/.env)
docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build   # locally: http://localhost:8080, API http://api.localhost:8080
docker compose -f docker-compose.yml -f docker-compose.local.yml logs -f backend  # follow one service
docker compose -f docker-compose.yml -f docker-compose.local.yml run --rm backend python -m app.seed   # seed inside the stack
docker compose -f docker-compose.yml -f docker-compose.local.yml down            # stop (volumes kept)
docker compose pull backend frontend && docker compose up -d                       # production (on the VM; CI deploys, see section 7)
```

Before every commit that touches `frontend/`: `typecheck`, `lint`, `test`, and `build` must all pass.
Before every commit that touches `backend/`: `ruff check`, `ruff format --check`, `mypy` and `pytest` must all pass (pytest needs the dev database container and `backend/.env`).

## 6.1 Frontend implementation notes

- **Routing**: every public page lives under a language prefix, `/en/...` or `/zh-hant/...` (`src/components/LanguageLayout.tsx`). `/` redirects using the saved language (`localStorage.lang`) or the browser language. The URL is the source of truth for the active language. The admin panel lives at `/admin/*`, without a language prefix (see Admin panel).
- **i18n**: locale config in `src/i18n/languages.ts`; UI strings in `src/i18n/locales/{en,zh-Hant}.json`. Every key must exist in both files.
- **Content**: the database, edited in the admin panel, is the source of truth for the page content of Experience, Projects, Skills and Contact (the storage model is in section 5; proper nouns stay untranslated, descriptive chips are translated terms, so no English phrase survives on the Chinese page). The sections render `ResolvedContent` (`src/content/resolved.ts`), exactly the `GET /content` payload of one locale. The static fallback is that payload saved per locale, `src/content/snapshot/{en,zh-Hant}.json`, bundled and returned by `staticContent(locale)` (`staticContent.ts`). `npm run content:sync` (`frontend/scripts/sync-content.mjs`) rewrites both files from a running API (default `http://localhost:8000`, else `VITE_API_BASE_URL` or `npm run content:sync -- <origin>`; `-- --api <origin>` works too, and Windows PowerShell drops the `--`, so the bare origin is accepted); it fetches and checks both locales (fetching once more if their untranslated facts disagree, i.e. an admin save landed in between), writes both to temporary files and renames them into place, so a failure leaves the previous pair untouched. File links (cover images, the CV) are ids in the database behind that API, so sync from the API of the database the site will use (production before a deploy; the script notes a local origin). After editing content in the admin panel, run it and commit the two files (Phase 7 automates this); a stale snapshot only means the first paint shows the old content until the API answers. `snapshot.test.ts` validates both files with the same zod schema as a live response, key for key (an extra key would make every identical API answer look new), and checks that both locales carry the same items in the same order with the same untranslated facts; no test depends on what the snapshot says (the other tests render the fixed fixture `src/test/contentFixture.ts`, see Frontend ↔ API). Only layout copy stays under `content.*` in the locale files (`locales.test.ts` pins the list): `content.techOf`, `content.experience.present`, `content.projects.{note,visit,placeholder.*}`, the `content.skills.credentials.*` labels and the in-progress suffix, `content.contact.labels.{cv,location}`, `content.contact.cvDownload`. Dates are stored once, as `YYYY-MM`, and the visible label is formatted per locale (`src/i18n/formatMonth.ts`), so `<time datetime>` and the label can never drift apart. Projects: a `placeholder: true` project is a reserved slot (badge, generic copy, never an image); the "being written up" note shows only while a slot remains (or there is no project); a real project's cover image (`image`, uploaded in the admin panel) fills the card's parallax plane over the hatch (`<img alt="">` with `object-cover`, lazy, decorative because the title and summary say what it is, keyed by its URL); an image that fails to load hides itself and leaves the hatch, never an empty frame. Contact: the channels (email, LinkedIn, GitHub, approved for publication by the owner) are content rows; once a CV is uploaded, a CV row with a download link (`Download CV (PDF, 180 KB)`, size from `src/i18n/formatFileSize.ts`) sits between the channels and the location. File URLs in the payload (`image.url`, `cv.url`) are root-relative paths on the API origin; `fileUrl()` (`src/api/files.ts`) prefixes `API_BASE_URL`.
- **Theme**: `data-theme="light|dark"` on `<html>`, set before first paint by `public/theme-init.js`, a blocking script in `index.html`'s head (a file, not an inline script, so the Content-Security-Policy needs no `'unsafe-inline'`), then managed by `src/theme/useTheme.ts`. Follows the OS until the user toggles; the choice is saved in `localStorage.theme`.
- **Design tokens**: defined once in `src/index.css` (CSS variables + Tailwind v4 `@theme`). Use the semantic utilities `bg-bg`, `bg-surface`, `text-fg`, `text-muted`, `border-line`, `text-accent`, `bg-accent`, `text-accent-fg`, and the `text-display` / `text-headline` sizes. Never use raw hex values in components. All text tokens are verified >= 4.5:1 contrast in both themes; re-check when changing a colour.
- **Fonts**: self-hosted via Fontsource (Inter Variable, Noto Sans TC Variable, IBM Plex Mono), with no Google Fonts requests.
- **SEO** (Phase 8): `index.html` holds what every page shares, for crawlers that run no script: the description, `theme-color` per colour scheme, `hreflang` alternates (`en` → `/en`, `zh-Hant` → `/zh-hant`, `x-default` → `/en`), Open Graph (`og:type`, `og:site_name`, `og:image` = `/og-image.png`, 1200×630, drawn by `frontend/scripts/og-image.py` with the site's fonts and palette; run it with the backend's Python) and `twitter:card`, and JSON-LD structured data (a `Person` with the three roles and the approved LinkedIn / GitHub links; a data block, never executed, so the CSP needs nothing for it). `DocumentMeta` (`src/components/DocumentMeta.tsx`, helpers in `src/seo.ts`) sets the language-specific parts in a layout effect: title, `meta.description` (both locales), the canonical URL, `og:title`, `og:description`, `og:url`, `og:locale` (`en_US` / `zh_HK`); canonical and Open Graph URLs are absolute on `SITE_ORIGIN` (`https://owwsolution.com`) whatever host serves the page. There is no static canonical (it would name `/en` on the Chinese page before the script runs). `public/robots.txt` allows everything but `/admin` and names `public/sitemap.xml` (both home pages, each with its alternates). The SPA answers unknown paths with 200, so `NotFoundPage` adds `<meta name="robots" content="noindex">` while mounted (no soft 404 in the index); the admin panel sends its own `noindex`. Lighthouse (production build): accessibility, SEO and best practices 100 in both languages. The ghost numerals are generated content (`data-numeral` + `::before`), decorative and never counted as low-contrast text. Performance: desktop about 97; Lighthouse's throttled mobile profile about 72–78, bounded by the scroll-driven design (the first commit runs every `useGSAP` setup and the first full layout in one task, and the hero name's mask reveal delays LCP by the intro): measured, accepted as the cost of the non-negotiable motion; Cloudflare's CDN serves the assets.
- **Animation**: GSAP + ScrollTrigger + SplitText (registered only in `src/animations/gsap.ts`; import gsap from there) and Lenis. `SmoothScrollProvider` (a pathless layout route in `App.tsx`, so it survives language switches) owns the single Lenis instance: `autoRaf: false`, driven by `gsap.ticker` with `lagSmoothing(0)`, `lenis.on('scroll', ScrollTrigger.update)`, no scrollerProxy or normalizeScroll, destroyed on unmount. Sections consume hooks in `src/animations/` (`useHeroIntro`, `useParallax`, `useSectionReveal`, `useStatementReveal`, `useScrollProgress`, `useActiveSection`, `useScrollTo`, `useHashScroll`); motion numbers live in `motion.ts`. Rules: the static DOM is the final state (no CSS ever hides content); from-states exist only inside a matched `gsap.matchMedia()` branch that requires `(prefers-reduced-motion: no-preference)`, and each branch runs through `runSafely()`, which reverts on error; animate only transform/opacity; parallax targets outer `[data-speed]` wrappers, intro and reveal tweens target inner elements, and GSAP targets never carry Tailwind translate/scale/rotate classes; exactly one pin (Statement, only on fine-pointer desktops at least `md` (48rem) wide and 600px tall where Lenis drives the wheel; on a child, never the root; `refreshPriority: 1` so it is refreshed before the triggers below it; no `anticipatePin`). `useScrollAnchor` keeps the reader's section when a breakpoint change rebuilds the matchMedia branches. Each content section passes `SectionShell` a `motionKey` (its item ids, joined): when the API payload adds or removes a card or row, `useSectionReveal` and `useParallax` revert and rebuild for the new set, so an item that only the API has is revealed and drifts like the others and no trigger stays on a removed node. Header offset: `--header-h` feeds `html { scroll-padding-top }` (Lenis and `scrollIntoView` both honour it) and `headerOffset()` in JS; never hard-code 64. Anchors: `SectionLink` renders a real `/lang#id` href; on the home page it smooth-scrolls via `useScrollTo` and focuses the section's `h2[tabindex="-1"]`; from other pages it navigates with router state that makes `useHashScroll` focus that heading; deep links land via `useHashScroll` before the first paint (no focus move); after a language switch focus returns to the language switcher, and the switch lands on the region being read (a section or the statement) at the same reading progress (`readingPosition.ts`; router state, applied on PUSH only, never replayed on Back/reload). JS media queries use the same unit as Tailwind (`MD_UP` / `BELOW_MD` in `media.ts`); never write px width queries. Reduced motion: no Lenis, no intro, parallax, pin, split or progress rule; native instant anchors. Touch tablets: no pin (the unpinned line reveal). Mobile (below `md`): no pin, halved parallax, no x-shear, native touch scroll (`syncTouch: false`); the menu is a native `<dialog>` with CSS-only motion. A scrubbed reveal never hides focus: `[data-reveal-inner]:focus-within` forces `opacity: 1` and `transform: none` (`!important` beats the inline styles GSAP writes), so a keyboard user never types into a translucent, displaced form or tabs onto a half-faded link.
- **Frontend ↔ API**: `src/env.ts` reads the two public build-time values `VITE_API_BASE_URL` (default `http://localhost:8000`, no trailing slash; production `https://api.owwsolution.com`) and `VITE_TURNSTILE_SITE_KEY` (default Cloudflare's always-pass test key `1x00000000000000000000AA`, never valid in production); both are compiled into the bundle, so a change needs a rebuild (`frontend/.env.example`). `src/api/client.ts` is the only fetch wrapper (`${base}/api/v1`, JSON in and out, `FormData` bodies sent as is, optional extra `headers` for preflighted routes only (the admin's `If-Match`), 15 s deadline, errors typed as `ApiHttpError` with the contract's `code` / `fields` / `Retry-After`, `ApiPayloadError`, `ApiNetworkError`); every 2xx body is validated with zod (`zod/mini`, `src/api/schemas.ts`; object schemas stay non-strict so keys the server adds later are ignored, and `contentPayloadSchema` is typed against `ResolvedContent` so the API shape and the static shape cannot drift; `projects[].image` and `cv` are required keys, `null` when absent, and their `url` must be a root-relative path, so a payload can never point an `<img>` or a download at another host; a project `url` the browser cannot parse becomes `null` (`z.catch`), costing that card its link, never the whole payload). Content: `useContent()` (`src/content/useContent.ts`, mounted once by `ContentProvider` in `HomePage`) returns the saved snapshot synchronously (`staticContent(locale)`), fetches `/content?locale=<active locale>` with `credentials: 'omit'` and no custom headers, swaps the payload in on success, keeps the reader in place across the swap (the section under the header and the reading progress are read with `readAnchor()` just before it and restored with `restoreAnchor()` in a layout effect, `src/animations/readingPosition.ts`, unless Lenis is mid-scroll; so a deep link or language switch that already landed stays landed even when the API answers after the landing's font correction), then calls `scheduleScrollTriggerRefresh()` (section heights change), keeps the static snapshot on any error / non-200 / zod failure / locale mismatch, and skips a payload identical to the static one (no re-render). Sections read `useResolvedContent()` (the provided content, or the snapshot outside a provider) and never import the snapshot files directly, so both paths render identically. Contact form: `src/components/ContactForm.tsx` (+ `FormField.tsx`): name / email / message, an off-screen honeypot named `website`, the Turnstile widget (`src/components/TurnstileWidget.tsx` + `useTurnstile.ts`, shared with the admin login: `@marsidev/react-turnstile`, theme follows `<html data-theme>` through `useDocumentTheme`, language `en` / `zh-tw` (lowercase: Turnstile does not know `zh-TW`), a token issued before a theme or language change is dropped), inline validation mirroring the backend limits, one always-mounted polite live region, success view that takes focus; "Send another message" returns focus to the Name field. While sending, the submit button is held with `aria-disabled`, never `disabled` (a disabled button drops keyboard focus to `<body>`); `onSubmit` ignores a second submit. It posts `{name, email, message, turnstileToken, website}` to `POST /contact` (`credentials: 'omit'`) and maps 202 → success, 422 → the named fields (a 422 that names no visible field → the generic error, widget kept), 429 → "too many attempts" (minutes from `Retry-After`), 400 `turnstile_failed` / 5xx / network → generic error + widget reset with the draft kept. Its copy lives under `contactForm.*`. Tests never reach the network and never read a developer's `.env` / `.env.local` (`test.env` in `vite.config.ts` pins both `VITE_*` values): `src/test/setup.ts` stubs `fetch` (queue responses with `src/test/api.ts`), replaces the Turnstile module with `src/test/FakeTurnstile.tsx`, and mocks `staticContent` with the fixed, invented `src/test/contentFixture.ts` (a current and a past role, a project with a cover image next to a slot, a translated chip, an in-progress certification, no CV), so re-syncing the snapshot never breaks a test; only `snapshot.test.ts` unmocks it to check the real files.
- **Admin panel** (`src/admin/`): its own lazy chunk for `/admin/*` (`App.tsx`), outside the smooth-scroll and language layouts; if the chunk fails to load (a stale chunk name after a deploy), the page reloads once, guarded by `sessionStorage['admin:reloaded']`; if it still fails, an `ErrorBoundary` shows `AdminLoadError` (public strings `adminLoad.*`) with a Reload button that clears the guard. Pages: `/admin` (dashboard: item counts, unread messages, the CV, a reminder to run `content:sync`), `/admin/login`, `/admin/content/:collection` (list with move up / down, which posts a reorder, and delete behind a confirmation), `/admin/content/:collection/new` and `…/:slug` (the editor), `/admin/messages?status=unread&page=N` (inbox: read / unread, delete) and `/admin/cv` (upload, replace or remove the PDF). The login page carries the shared Turnstile widget: the submit is blocked (with a message on the widget, announced) until it has a token, the token is sent as `turnstileToken`, and any failed attempt (401, 400 `turnstile_failed`, 429 (seconds under a minute, else minutes), 503, network) resets the widget, since a token is single-use; a 422 keeps it unless it names `turnstileToken`. `RequireAuth` asks `GET /auth/me` once on entry; a 401 from any later call returns to the login page with the way back and a "session expired" notice. Every call goes through `src/admin/api.ts` → `requestJson` with `credentials: 'include'` (JSON bodies; uploads as `FormData` on PUT with a 120 s deadline), and responses are validated by `src/admin/schemas.ts`: JSON types only, so a row that breaks today's rules can still be opened and repaired, and non-strict objects, so a key the server adds later never flows back into a write. The editor is driven by the collection configs in `collections.ts` (fields, labels, hints, list title and detail) and checks the backend's limits before sending (`validation.ts`; lengths in code points, as Python counts them); Server answers are shown in the panel's own words, never the server's English: a 422 marks the control its dotted field names with "the server rejected this value" (`body` and keys that match no control become form-level errors, the latter with the path), a 409 lands on the slug, a 404 on save says the row was deleted elsewhere, a 413 says the entry is too large. The panel never sends `sortOrder` (the server places new rows and keeps the order on edits). An edit is saved with `If-Match: "<updatedAt>"`, the version it was loaded from (then of each save it stays on); a 412 keeps the changes on screen and in the tab and offers "Load latest version", which, after a confirmation, replaces the form with the server's row. The slug is fixed once created, and `new` is refused (it is the create route; the backend refuses it too). Cover images and the CV are saved at once, apart from the form (8 MB / 10 MB and the type checked before sending; one upload at a time: a pick made while one is on its way is refused, since aborting it would not stop the server); a refusal is shown by its `code` (the API surface list; an unknown one gets a generic "could not use this file"), a 413 as the size message. An image change leaves the project's `updatedAt` alone on the server, and the editor takes only the `image` from its answer (applied to the row as it is when the answer lands), so a change saved elsewhere meanwhile is still refused on save. Project links are checked like the backend does (the case-sensitive `http(s)://` prefix, a host without forbidden characters, a port from 1 to 65535, then the browser's parser). A placeholder project shows a note instead of the upload control. The inbox passes the first page's `asOf` to later pages (in the URL and the request), so paging through never shifts; page 1 and a filter switch start afresh (a refresh after a delete still passes the moment the page was taken at), and an out-of-range page or an `asOf` the API would refuse falls back to page 1. Unsaved drafts are kept per tab in `sessionStorage` (`admin:draft:<id>`, gone with the tab) and offered back after an expired session, a reload or Back / Forward; kept drafts are versioned (`{v: 1, …}`, other versions are ignored); in-app links, signing out and closing the tab ask before dropping changes, and the draft is dropped only once that action succeeded (a failed sign-out keeps everything) (Back / Forward cannot be guarded: the declarative router has no blocker). The page heading takes focus on every in-app navigation (not on Back / Forward); load errors are `role="alert"` with a Retry that moves focus to the heading and announces "Loaded." on success; a failed refresh keeps the loaded list with a notice. Strings live in the i18next namespace `admin` (`src/admin/locales/{en,zh-Hant}.json`, registered when the chunk loads, so none ship in the public bundle; `src/admin/locales.test.ts` checks key parity, placeholders, plurals, that Chinese is never a copy of the English and that every `t('…')` key exists). The language toggle shares `localStorage.lang` with the public site; dates use `zh-Hant-HK`. While mounted, the panel sets `<html lang>` and `<meta name="robots" content="noindex, nofollow">`. File URLs and sizes use the public helpers (`fileUrl`, `formatFileSize`). Tests (`src/admin/**/*.test.ts(x)`) run against a mocked API (`src/admin/test/mockApi.ts`, `fixtures.ts`; invented data only).

## 6.2 Backend implementation notes

- **Layout**: `app/core` (settings, db engine/session factory, security, rate limiting, client IP, error envelope, security headers), `app/models` (SQLAlchemy 2 declarative with a naming convention and `TimestampMixin`), `app/schemas` (Pydantic v2: `CamelModel` / `StrictCamelModel`, `Localized[T]`, `Tag`, the public `ContentPayload`, the write models `*In` and their admin read models `*Out` in `admin.py`, request and error models), `app/services` (content, content_admin + the `collections` registry, files, messages, contact, turnstile, email, auth), `app/api/deps.py` + `app/api/v1/*` (routers), `app/seed`, `alembic/` (async env; uses `sqlalchemy.url` from the Alembic config when set, else `DATABASE_URL`, so tests point it at `TEST_DATABASE_URL`), `tests/`. `app/main.py` exposes `create_app(settings)` and, through a module `__getattr__`, `app` (the ASGI entry point for `uvicorn app.main:app`), built on first access: importing the module reads no settings, so the tests (and CI, which has no `backend/.env`) import `create_app` with their own; the lifespan creates the engine and one shared `httpx.AsyncClient`, upserts the admin (`services.auth.ensure_admin_user`) and stores everything in `AppServices` (`core/state.py`) that the dependencies read. Migrations are never run at startup.
- **API surface**: everything under `/api/v1`, no trailing slashes (`redirect_slashes=False`), camelCase JSON, OpenAPI docs at `/api/v1/docs` and the schema at `/api/v1/openapi.json`, both switched off when `APP_ENV=production`. Every non-2xx response, on every route, is `{"error": {"code", "message", "fields"?}}` (`core/errors.py` replaces FastAPI's default handlers; codes: `validation_error` 422, `not_found` 404, `method_not_allowed` 405, `payload_too_large` 413, `turnstile_failed` 400, `invalid_credentials` 401, `unauthorized` 401, `forbidden` 403, `conflict` 409, `precondition_failed` 412, the upload refusals 422 (`file_missing`, `file_empty`, `file_too_large`, `file_type`, `image_unreadable`, `image_too_many_pixels`, `image_placeholder`, each with the text in `fields.file`), `rate_limited` 429 + `Retry-After`, `internal_error` 500, `service_unavailable` 503). `message` (and a `fields` text) is developer-facing; the frontend maps `code` to its own i18n strings. An unhandled exception is answered by `ServerErrorEnvelopeMiddleware` (`core/middleware.py`, innermost) with the 500 envelope, so it still passes through CORS and the security headers (a cross-origin caller sees a server error, not a network failure), then re-raised so the server logs the traceback. Routes: `GET /health` (`SELECT 1`), `GET /content?locale=en|zh-Hant` (default `en`; every string resolved for that locale, plus `projects[].image` `{url, width, height}` and `cv` `{url, filename, size, updatedAt}`, each `null` when absent; compact UTF-8 body, strong sha256 `ETag`, `Cache-Control: public, max-age=60`, 304 on a matching `If-None-Match`; the rendered document is kept per locale in `AppServices.content_cache` (`ContentCache`, `services/content.py`) for 30 s, a miss built once however many requests wait for it, and every admin write (a dependency on the admin router) clears it, so edits show at once; a build overtaken by a write is served but not kept), `GET /files/{id}` (see Files), `POST /contact` (202), `POST /auth/login`, `POST /auth/logout` (204), `GET /auth/me`, and the admin API under `/admin` (see Admin API). Lists are ordered `sort_order ASC, slug ASC`; a slug becomes `id` in the payload. Every response also carries `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy` and `Permissions-Policy`, and every response under `/api/v1/admin` and `/api/v1/auth` (errors included) `Cache-Control: no-store` unless it sets its own (`SecurityHeadersMiddleware`, `core/middleware.py`); HSTS is Caddy's job. `RequestBodyLimitMiddleware` (same file) caps every request body at 64 KiB (`MAX_REQUEST_BODY_BYTES`; the largest public request is about 20 KB) except where `route_limits` says otherwise (full-match path patterns set in `main.py`, first match wins: `PUT /admin/content/projects/<slug>/image` 9 MiB = the 8 MiB image limit + 1 MiB of multipart framing, `PUT /admin/cv` 11 MiB, and every other `/admin/content/…` write 512 KiB, since a maximal bilingual experience entry is 80–100 KB of JSON); a declared `Content-Length` over the cap is answered 413 before routing (no rate-limit slot, no database work), and a chunked body is counted as it streams and cut off with the same 413; both carry `Connection: close`, so the server drops the rest of the body. It sits inside CORS and the security headers, so the 413 carries both.
- **Settings** (`core/config.py`; field name = env var; `backend/.env` is read automatically; keep `backend/.env.example` in sync): `APP_ENV` (`development` | `test` | `production`), `DATABASE_URL`, `TEST_DATABASE_URL` (pytest only), `JWT_SECRET` (at least 32 characters), `IP_HASH_SECRET` (at least 32 characters; the HMAC key for contact-message IP hashes; changing it makes old hashes unmatchable), `ADMIN_EMAIL`, `ADMIN_PASSWORD`, `CONTACT_TO_EMAIL`, `EMAIL_FROM` (default `noreply@owwsolution.com`), `EMAIL_PROVIDER` (`console` | `resend`; `resend` requires `RESEND_API_KEY`), `TURNSTILE_SECRET_KEY`, `CORS_ORIGINS` (comma-separated exact origins, `*` rejected), `TRUSTED_PROXY` (default `false`). For a one-off run, override in the environment instead of editing `.env` (environment variables win). Settings errors never echo input values (`hide_input_in_errors=True`), so a failed start cannot print a secret. With `APP_ENV=production` the process refuses to start (`_refuse_unsafe_production`, every problem listed, no value shown) on a `change-me` placeholder in `JWT_SECRET`, `IP_HASH_SECRET` or `ADMIN_PASSWORD`, an `ADMIN_PASSWORD` under 12 characters, `JWT_SECRET == IP_HASH_SECRET`, a Cloudflare test Turnstile secret, any `EMAIL_PROVIDER` but `resend`, or an empty `CORS_ORIGINS`.
- **Auth, cookie, CORS**: one admin row, upserted at startup from `ADMIN_EMAIL` / `ADMIN_PASSWORD` (Argon2id via argon2-cffi with OWASP's baseline, m = 19 MiB, t = 2, p = 1: the library default takes 64 MiB per check, which a burst of logins would turn into an out-of-memory kill on the 1 GiB VM; always run in the thread pool with `run_in_threadpool` so a login never stalls the event loop; email lowercased). The startup upsert (`ensure_admin_user`) keeps exactly one admin: it deletes every other `admin_users` row (a previous `ADMIN_EMAIL` loses its password and its sessions), and when the configured password no longer verifies it re-hashes it and bumps `session_version`, revoking every session issued under the old password; a hash made with other parameters is re-hashed without revoking anything. `POST /auth/login` (`{email, password, turnstileToken}`) runs the IP limiter (which also bounds the calls to Cloudflare), then Turnstile (400 `turnstile_failed`, 503 `service_unavailable`; an attempt without a solved challenge neither reaches the password check nor counts toward the email limits, so the public admin email cannot be locked out with bogus tokens), then the email limiters, then waits up to 5 s for one of two password-check slots (`AppServices.password_checks`, an `asyncio.Semaphore`; a flood queues instead of exhausting memory, and a request that finds no slot gets 429 with `Retry-After: 5`), then a constant-time credential check (an unknown email verifies against a dummy hash computed at startup), and answers 401 `invalid_credentials` identically for unknown email and wrong password. Success sets `admin_session` = HS256 JWT (`sub` = user id, `email`, `ver` = the admin's `session_version`, `typ=admin_session`, `iat`, `exp` = 12 h) with `Path=/api; Max-Age=43200; HttpOnly; SameSite=Lax`, plus `Secure` when `APP_ENV=production`, no `Domain`; logout sends the same attributes with an empty value, `Max-Age=0` and `Expires` in 1970, and when the request still carries a valid session it bumps `session_version`, which revokes every copy of it (another tab or device, a leaked cookie). `GET /auth/me` and every admin route use the admin session dependency (`CurrentAdminDep` / `get_current_admin`; 401 `unauthorized` for a missing, invalid, expired, orphaned or revoked token: `ver` must equal the row's `session_version`); it commits after the lookup, so an upload's body is received without a pooled connection idle in a transaction. All of them answer with `Cache-Control: no-store` (the middleware above). CORS (`CORSMiddleware`): `CORS_ORIGINS` with credentials, `expose_headers=["ETag", "Retry-After"]`, `max_age=600`; the frontend sends `credentials: 'include'` only on `/auth/*` and `/admin/*`. CSRF protection, in layers: the SameSite=Lax cookie; exact CORS origins; `require_trusted_origin` on the `/auth` and `/admin` routers (a POST / PUT / PATCH / DELETE whose `Origin` header is present and not in `CORS_ORIGINS` is refused 403 `forbidden`; a request without `Origin`, such as curl, is left to the cookie check); JSON bodies (FastAPI reads a body as JSON only for `application/json`, so a `text/plain` or urlencoded form post fails validation); and uploads only on PUT, which no HTML form can send. Keep all four for every new mutating route. Client IP (`core/client_ip.py`): the rightmost `X-Forwarded-For` entry only when `TRUSTED_PROXY=true` (behind Caddy), otherwise the socket peer, otherwise the literal `unknown`; the raw IP stays in memory (rate-limit keys, Turnstile's `remoteip`); what is stored, emailed or logged is `hash_client_ip()`: HMAC-SHA256 keyed with `IP_HASH_SECRET` over the canonical address (IPv4-mapped IPv6 folded to IPv4), 64 hex characters, shown as its 12-character `fingerprint()` in emails and logs.
- **Rate limiting** (`core/rate_limit.py`): an in-memory sliding window per key (a deque of timestamps; rejected requests are not recorded; `Retry-After` = whole seconds until the oldest counted event leaves the window). A key's `<ip>` is `rate_limit_subject(ip)` (`core/client_ip.py`): the IPv4 address, or the /64 network of an IPv6 address (one subscriber usually holds a whole /64; behind Cloudflare visitors arrive over IPv6 too), while Turnstile and the stored hash keep the full address. Rules, every one counted on arrival (so a parallel burst cannot slip past a check made before its failure is recorded): `contact:ip:<ip>` 5 / 15 min before body validation (a dependency on the route), `login:ip:<ip>` 10 / 15 min, `login:email-ip:<email>:<ip>` 5 / 15 min (the brute-force limit) and `login:email:<email>` 30 / 15 min (a distributed guess), both cleared by a successful login. The admin email is public (it is the contact address), so failures from one address never lock the owner out elsewhere, and each attempt needs a solved Turnstile challenge. Single-instance caveat: the counters live in one process, so run exactly one Uvicorn worker (Phase 4/6); several workers or replicas would each keep their own counters, and a shared store (Redis) is a later decision. Tests override `get_rate_limiter` and call `reset()`.
- **Contact flow** (`services/contact.py`): rate limit → validate `ContactRequest` (trimmed; `name` 1–100, `email` valid and ≤ 254, `message` 10–5000, `turnstileToken` 1–2048, `website` honeypot default `""`; unknown keys rejected) → honeypot filled ⇒ 202 and nothing else (logged at INFO with the source fingerprint, never the IP) → Turnstile siteverify (`services/turnstile.py`, 5 s timeout, `remoteip` omitted when unknown; in production a token must also have been solved on one of the `CORS_ORIGINS` hosts (`site_hostnames`), so a token from another site sharing the widget is refused; `success:false` ⇒ 400 `turnstile_failed`, unreachable / non-2xx / unparsable ⇒ 503 `service_unavailable`) → insert `contact_messages` (name, email, message, `ip_hash`, `user_agent` truncated to 512 characters: the owner approved storing the hashed IP and the User-Agent; the `ck_contact_messages_ip_hash_format` check only admits a 64-hex hash, so a raw IP can never land there) with `delivery_status='pending'` (DB failure ⇒ 500) → send through the email provider → mark `sent` + `delivered_at`, or `failed` + `delivery_error` (logged at ERROR); if only that status update fails, the row stays `pending`, the error is logged and the visitor still gets the 202 (a 500 would make them send it again) → 202 `{"status":"accepted"}` either way, because the visitor must never learn whether delivery worked. Providers (`services/email.py`, `EmailProvider` protocol, chosen by `EMAIL_PROVIDER`): `console` logs the whole message at INFO (development), `resend` posts to `https://api.resend.com/emails` with `Authorization: Bearer RESEND_API_KEY` (10 s timeout, non-2xx ⇒ `EmailDeliveryError`). The message goes to `CONTACT_TO_EMAIL` from `EMAIL_FROM` with reply-to = the visitor, subject `[owwsolution.com] New message from <name>`, text = name (on one line, as in the subject: a name with line breaks cannot add fake `Email:` / `Source:` lines), email, `Source: <fingerprint> (hashed IP)`, received-at (UTC ISO 8601), blank line, message. Owner decisions: messages are kept until the owner deletes them (no automatic expiry; the Phase 5 admin panel lists and deletes them), and the visitor sees success even when delivery failed, because the message is already safe in the database.
- **Admin API** (`api/v1/admin.py`; every route needs the admin session, runs `require_trusted_origin` and answers `Cache-Control: no-store`): `GET /admin/summary` (items per collection, unread messages, the CV). Content collections are addressed by URL segment through the registry in `services/collections.py` (`experience`, `projects`, `skill-groups`, `education`, `certifications`, `languages`, `contact-links`, `site-texts`), which pairs each model with its write model (`*In`, `schemas/content_write.py`), its read model (`*Out` = the write model plus read-only `updatedAt`, and `image` on projects; `sortOrder` always present) and the row mapping the seed shares. `GET /admin/content/{collection}` → `{"items": [...]}` in both locales, in display order; `POST` (201, 409 `conflict` on a taken slug, 422 on `slug` for `new`, the admin panel's create route); `PUT …/{slug}` (full replacement; the slug is immutable, another one in the body is 422 on `slug`); `DELETE …/{slug}` (204; a project's cover image file goes with it); `POST …/reorder` `{"slugs": [...]}` (every slug exactly once, else 422 on `slugs`; rewrites `sort_order` to 0..n-1; `site-texts` has no order, 404). The order belongs to the server: `sortOrder` is optional in the write models and the admin panel never sends it; a new row without it is placed by the server (an experience entry by chronology, current roles first and then the latest `start` first, the rows after it moving down one; anything else after the last), an update without it keeps the stored order, and order changes (placement, reorder) and cover image changes never touch `updated_at`, which stays the version of the row's editable content. Optimistic concurrency: a `PUT` may carry `If-Match: "<updatedAt>"` (the version it was edited from, as the API returned it); the row is locked, and if its `updated_at` differs (or the header is not one aware timestamp) the answer is 412 `precondition_failed` and nothing is saved; `*` or no header skips the check. Bodies are validated against the collection's write model inside the handler and keep the 422 envelope with dotted `fields` keys (`translations.zh-Hant.role`, `tech.2`; `body` for rules across fields); the `Tag` validator reports one error per chip, on its list index. Input validation spells out ASCII (`[0-9]`: Pydantic's regex engine would take any Unicode digit for `\d`), bounds `sortOrder` to int4 and message ids / `offset` to the database's ranges, and accepts a project `url` only when a browser could parse it (scheme http(s), a host without forbidden characters, a valid port, no whitespace). Project cover image: `PUT /admin/content/projects/{slug}/image` (multipart, field `file`) and `DELETE …/image`, both answering the project (`ProjectOut`); a placeholder takes no image (422 `image_placeholder`), and a project with an image cannot become a placeholder (422 on `placeholder`). CV: `GET`, `PUT` (multipart `file`), `DELETE /admin/cv`, all answering `{"cv": {...} | null}`. Upload refusals are 422 with the specific codes listed under API surface. Messages: `GET /admin/messages?status=all|unread&limit=1..100&offset=0..&asOf=<iso>` → `{items, total (for the filter), unread (overall, live), asOf}`, newest first: the set is taken as it was at `asOf` (now when absent, and the response says which instant it used), leaving out later messages and, in the unread view, still counting messages read after it, so the later pages of a view that pass the first page's `asOf` never shift when messages are read or arrive; an item carries `source` (the 12-character fingerprint of the IP hash, never the hash itself), `userAgent`, `deliveryStatus` / `deliveryError` and `readAt`; `PATCH /admin/messages/{id}` `{"read": bool}` (reading again keeps the first read time); `DELETE` (204). An upload is read inside the handler, after the auth dependencies (`read_upload`), never as a FastAPI form parameter, which would be parsed before authentication.
- **Files** (`models/stored_file.py`, `services/files.py`, `api/v1/files.py`; migration `0002_admin_panel`): uploads live in PostgreSQL, table `stored_files` (uuid id, `kind` `cv` | `project_image`, content type, original filename, size, sha256, width / height for images, `data` bytea loaded only when served; CHECKs on the kind, the sha256 format, `size = octet_length(data)` and the dimensions; a partial unique index admits one CV). `projects.image_id` references it (unique, `ON DELETE SET NULL`), and the placeholder CHECK also requires `image_id IS NULL`. A cover image is JPEG (including the MPF / "MPO" JPEGs of phones and cameras), PNG or WebP by its signature (anything else, garbage included, is 422 `file_type` before Pillow parses a byte) of at most 8 MiB and 40 megapixels (checked from the header, before decoding, with only the JPEG, PNG and WebP readers enabled, so no other format's parser ever runs on an upload); in the thread pool a JPEG is decoded at the smallest scale that still yields 1600 px after the camera's rotation (`draft`), then the image is turned upright from its EXIF orientation (malformed metadata is ignored, the pixels kept), 16-bit greyscale is scaled to 8 bits (a plain conversion would clip it to white), metadata is dropped but an RGB colour profile (Display P3, Adobe RGB) is kept, and it is scaled to fit 1600 px wide and 16383 px tall (WebP's limit per side) and stored as WebP (quality 82); anything Pillow cannot read or encode is a 422 `image_unreadable`, never a 500. The CV is one PDF (`%PDF-` signature) of at most 10 MiB; a new one replaces the old in one transaction under an advisory lock (`pg_advisory_xact_lock`), and its download name is the uploaded filename sanitised (no path, control or quote characters, `.pdf` ensured, at most 120 characters; `CV.pdf` by default). Replacing or removing an image, or deleting its project, deletes the old file; the project row is locked (`FOR UPDATE`) for it, so overlapping uploads replace each other in turn and leave no orphan. `GET /files/{id}`: 404 for a malformed or unknown id (the metadata is read on every request, so a removed file is gone at once); `ETag` = the sha256, 304 on `If-None-Match` without loading the body; the body comes from `FILE_BODY_CACHE` (an in-process LRU of 64 MiB: concurrent downloads share one copy, the database sends a body once) or the database; cover images `inline` with `Cache-Control: public, max-age=31536000, immutable` (a new upload always gets a new id), the CV (personal data) `attachment` with an ASCII `filename` and a UTF-8 `filename*` and `Cache-Control: private, no-cache` (no shared cache keeps it, a browser revalidates, so a removed CV stops being served).
- **Seed** (`app/seed/`): `content.json` is the site's initial content in the storage shapes (`translations` objects, tag strings or `{"en","zh-Hant"}` term objects, `YYYY-MM` dates); rows are validated by the write models and mapped by the collection registry. `seed_content` upserts every row by slug in one transaction (`INSERT … ON CONFLICT (slug) DO UPDATE`, `updated_at = now()`; a row without `sortOrder` takes its position in the file), never deletes a row and keeps a project's image, unless the file makes that project a placeholder (which cannot have one): then the image is dropped, and cover images no project points at are deleted. The admin panel is the source of truth, so `content.json` is not kept in step with it and only bootstraps an empty database (development, a new server): `python -m app.seed` refuses a database that already holds content, because it would put the file's version back over every edited row; `--force` seeds anyway.
- **Tests** (`tests/`, 330 cases): pytest + pytest-asyncio (auto mode) + httpx `ASGITransport` against `TEST_DATABASE_URL` (a real PostgreSQL: schema dropped and `alembic upgrade head` once per session, every table truncated before each test); the real lifespan runs with Turnstile and email replaced by recording fakes through dependency overrides; no network anywhere. Only `TEST_DATABASE_URL` is read from `backend/.env`; every other setting is a test constant, so no personal value appears in test output. Coverage includes migrations at head and model drift, seed idempotency and the CLI's refusal, `/content` field by field against `content.json` for both locales plus ETag / 304, every contact path, the IP hash (never a raw IP in the row, the email or the log), login / cookie / `/me` / logout, session revocation, the single-admin invariant, Argon2 off the event loop, the production settings guard, the body caps (64 KiB, per-route caps that do not leak to other routes, `Connection: close` on a streamed 413), the 500 envelope with CORS headers, rate limiting, the error envelope, CORS and the security headers; for the admin API: 401 on every route, the foreign-origin 403, JSON-only writes, `no-store` on successes and errors, a round trip of every collection, validation field paths, ASCII digits and integer ranges, project URL parsing, slug conflicts and immutability, server-side placement and order keeping, `If-Match` / 412, reorder and image changes without a version bump, the summary, the upload pipeline (`test_images.py`, no database: tall screenshots, MPO JPEGs, rotated JPEGs at full width, broken metadata, broken PNG streams, 16-bit greyscale, colour profiles, refusal codes), file cleanup, overlapping uploads, the CV rules, download names and caching, and the message list, filter, paging with `asOf`, read state and delete; the seed's `--force` over a slot that gained an image. Upload tests build their images with Pillow and their PDFs as bytes; no fixture file is committed.

## 6.3 Containers (Phase 6)

- **Stack** (`docker-compose.yml`, project `anthonyzng-web`): `db` (postgres:17-alpine, volume `pgdata`, no host port), `migrate` (the backend image, pulled or built by `backend` and never on its own (`pull_policy: never`), running `alembic upgrade head` once per `up`, then exiting; `restart: "no"`), `backend` (waits for `db` healthy and `migrate` completed successfully), `frontend`, `proxy` (caddy:2.11-alpine, the only published service: 80, 443, 443/udp; volumes `caddy_data` for certificates and `caddy_config`). Images: `ghcr.io/anthonyzng/anthonyzng-web-{backend,frontend}:${IMAGE_TAG:-latest}` (CI pushes them, section 7); the `build` sections serve local builds, which the local override tags `anthonyzng-web-{backend,frontend}:local`. Every long-running service has `restart: unless-stopped`, `no-new-privileges` and `pids_limit: 200`; `backend`, `migrate` and `frontend` run with a read-only root filesystem (`/tmp` as tmpfs) and `cap_drop: [ALL]`; `proxy` drops every capability but `NET_BIND_SERVICE`; `db` keeps its defaults (its entrypoint switches to the postgres user). Memory caps for the 1 GiB VM: `db` 256 MB, `migrate` 256 MB, `backend` 384 MB, `frontend` 64 MB, `proxy` 128 MB, so a runaway service is killed and restarted instead of taking PostgreSQL with it. `APP_ENV` is `production` unless the root `.env` or the local override says otherwise (a `backend/.env` that lost that line cannot start with development defaults). Configuration: the root `.env` (Compose interpolation: Postgres credentials, the Caddy addresses, the frontend build args; `.env.example`) and `backend/.env` (the backend settings, loaded with `env_file`); the compose file itself sets the backend's `DATABASE_URL` (host `db`, built from the `POSTGRES_*` values, so the password must be URL-safe) and `TRUSTED_PROXY=true`, overriding `backend/.env`. `docker compose config` prints the merged environment, secrets included: use `--quiet` to validate it.
- **Backend image** (`backend/Dockerfile`): `python:3.13-slim`; uv (`ghcr.io/astral-sh/uv:0.12`) installs the locked runtime dependencies only (`uv sync --frozen --no-dev --no-install-project`) into `/opt/venv`; the runtime stage copies the venv, `alembic.ini`, `alembic/` and `app/` (root-owned) and runs as the unprivileged `app` user (uid 10001). `CMD`: `uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 1 --no-access-log --no-server-header --limit-concurrency 64` (one worker for the in-process rate limiter; no access log, which would record raw client IPs; beyond 64 requests in flight uvicorn answers 503 at once instead of queueing without bound). `HEALTHCHECK` calls `/api/v1/health`. `.dockerignore` keeps `.env`, `.venv`, `tests/` and caches out, so no secret is ever baked into an image. The same image runs the seed: `docker compose … run --rm backend python -m app.seed`.
- **Frontend image** (`frontend/Dockerfile`): `node:22-alpine` runs `npm ci` and `npm run build` (typecheck + Vite) with the build args `VITE_API_BASE_URL` and `VITE_TURNSTILE_SITE_KEY` (both required: the build fails on an empty value instead of falling back to the development defaults of `src/env.ts`); the runtime is `nginxinc/nginx-unprivileged:1.29-alpine` on port 8080 with `frontend/nginx.conf`: `/assets/*` (hashed) `Cache-Control: public, max-age=31536000, immutable` and a 404 for a missing file, any other path with a file name (a dot in its last segment) that file or a 404 (a missing `/robots.txt` is never the app with a 200), every other path `index.html` (the SPA routes) with `no-cache`, `/healthz` for the healthcheck, gzip (`gzip_proxied any`: every request arrives through Caddy), `server_tokens off`, `absolute_redirect off`.
- **Proxy** (`proxy/Caddyfile`): site addresses from the environment (`SITE_ADDRESS`, `SITE_URL`, `WWW_ADDRESS`, `API_ADDRESS`; production defaults `owwsolution.com`, `https://owwsolution.com`, `www.owwsolution.com`, `api.owwsolution.com` in the compose file, so a bare domain gets an automatic Let's Encrypt certificate). The site host carries the frontend's security headers (`X-Frame-Options: DENY` and CSP `frame-ancestors 'none'`, so the admin panel can never be framed; `nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy`, HSTS one year, no `Server`) and a Content-Security-Policy: scripts only from the site, Cloudflare Turnstile (`challenges.cloudflare.com`, also the only `frame-src`) and Cloudflare Web Analytics (`static.cloudflareinsights.com`, beacons to `cloudflareinsights.com`), no inline script; styles only from the site (React and GSAP write styles through the CSSOM, which `style-src` does not govern); images and API calls from the site and `API_URL` (cover images are `GET /files/{id}`); fonts from the site and `data:` (Vite inlines font subsets under 4 KB); `object-src 'none'`, `base-uri 'self'`, `form-action 'self'`. A new third-party script, frame, image or API origin needs its CSP entry, and a change is checked in the browser console on the local stack (a violation is logged). The API host sends `default-src 'none'; img-src 'self'; frame-ancestors 'none'`; `www` redirects permanently to `SITE_URL` (one canonical origin); the API host adds HSTS, drops `Server`, caps request bodies at 11 MiB (`MiB`: Caddy's `MB` is decimal) and proxies to `backend:8000` without compression (it would rewrite the ETags). Behind Cloudflare: `trusted_proxies` lists Cloudflare's published ranges (refresh them when Cloudflare announces new ones) and `client_ip_headers CF-Connecting-IP`, so the visitor's address is taken from that header only when the request really comes from Cloudflare; every route to the backend (`to_backend` snippet, and the local site route) sets `X-Forwarded-For` to exactly that `{client_ip}`, the rightmost entry the backend reads with `TRUSTED_PROXY=true` (Caddy's default would append Cloudflare's edge address). Server timeouts: `read_header 10s`, `read_body 2m` (an 11 MiB CV on a slow link), `write 2m`, `idle 2m`, so a slow client cannot hold connections open. No access log anywhere. `caddy validate` / `caddy fmt` must stay clean (run them in the `caddy` image).
- **Locally** (`docker-compose.local.yml`, an override): plain HTTP on `127.0.0.1:8080` (`ports: !override`), `http://localhost:8080` (site) and `http://api.localhost:8080` (API). Browsers take `localhost` and `api.localhost` for two different sites, so the admin's `SameSite=Lax` cookie would not reach `api.localhost`: the local build therefore calls the API on the site's own origin (`VITE_API_BASE_URL=http://localhost:8080`, and the override mounts `proxy/local/api-on-site.caddy` into `/etc/caddy/site.d/`, which the site block imports, so Caddy routes `/api/*` of the site host to the backend; production mounts nothing there). Its images are tagged `:local`, so a local build never stands in for a GHCR image. The backend keeps its development `backend/.env` with `CORS_ORIGINS=http://localhost:8080`; the database is the same service and volume as `docker-compose.dev.yml` and stays published on `127.0.0.1:5432` for host-run tests; stop a host-run backend first so only one admin edits it.

---

## 7. Deployment

- **Where**: GCP project `anthonyzng-web` (billing linked), VM `anthonyzng-web` in `us-east1-b`: e2-micro (free tier; 1 GiB RAM plus 2 GiB swap), Debian 12, 30 GB standard disk, Shielded VM, network tag `web`; static external IPv4 `anthonyzng-web-ip` = `34.26.232.31` (a static address costs the same as an ephemeral one while in use, and DNS never has to change). Firewall (`default` network): `allow-web-cloudflare` (tcp 80, 443, udp 443 from Cloudflare's ranges → tag `web`; the former open `allow-web` is disabled), `default-allow-ssh` (22, key-only logins), ICMP and internal; the default RDP rule was deleted. The owner's gcloud CLI is signed in on the dev machine (`gcloud config`: project `anthonyzng-web`, zone `us-east1-b`); `gcloud compute ssh anthonyzng-web` gives an admin shell.
- **VM setup** (once, `deploy/vm-setup.sh`, run as root with the deploy public key): Docker Engine + Compose plugin from Docker's apt repository, 2 GiB swap (`vm.swappiness=10`), the `local` log driver (10 MB × 3 per container), unattended security upgrades, the `deploy` user (no password, login only with a `restrict`ed key, in the `docker` group, which makes it root-equivalent on the VM: the key is guarded accordingly) and `/opt/anthonyzng-web` (`750`, owned by `deploy`) with `backend/` and `proxy/`. The VM's SSH host key is pinned in the `DEPLOY_KNOWN_HOSTS` secret (ED25519 `SHA256:pY1Yg2Z6ho87hgcoEHiFYYqIpJTOOgtjyLkU7MZa/pY`, cross-checked over the gcloud channel and a fresh scan).
- **Production configuration lives only on the VM**: `/opt/anthonyzng-web/.env` (`POSTGRES_*`) and `/opt/anthonyzng-web/backend/.env` (the production backend settings, section 6.2: `APP_ENV=production`, separately generated `JWT_SECRET` / `IP_HASH_SECRET`, the admin account, `CONTACT_TO_EMAIL`, `EMAIL_PROVIDER=resend` + `RESEND_API_KEY`, the real `TURNSTILE_SECRET_KEY`, `CORS_ORIGINS=https://owwsolution.com`), mode `600`, owned by `deploy`. They are written on the VM and never pass through GitHub, the repo or a log. `docker-compose.yml` and `proxy/Caddyfile` are copied there by every deploy.
- **CI/CD** (`.github/workflows/ci-cd.yml`): every pull request and push to `main` runs `frontend` (npm ci, typecheck, lint, test, build), `backend` (uv sync, ruff, format check, mypy, pytest against a PostgreSQL 17 service) and `containers` (both Compose files parse, the Caddyfile validates and is formatted). On `main`, when the repository variable `DEPLOY_ENABLED` is `true`: `images` builds both images with Buildx (GHA cache) and pushes `ghcr.io/anthonyzng/anthonyzng-web-{backend,frontend}` tagged with the commit SHA and `latest` (private packages; the frontend gets the variables `VITE_API_BASE_URL` and `VITE_TURNSTILE_SITE_KEY` as build args), then `deploy` (GitHub environment `production`, one at a time) logs in as `deploy` with the `DEPLOY_SSH_KEY` secret and the pinned host key, copies the stack definition, logs the VM in to GHCR with the job's own `GITHUB_TOKEN` (valid only while the job runs; logged out right after the pull, so no registry credential stays on the VM), pulls this commit's images (`IMAGE_TAG=<sha>`) and tags them `latest` on the VM (so every manual compose command there uses the deployed images without a registry login), runs `docker compose up -d --remove-orphans` (the `migrate` service applies migrations first), reloads Caddy (a changed Caddyfile does not recreate its container), removes every image no container uses (`docker image prune -af`: the 30 GB disk never fills with old tags) and smoke-tests `https://api.owwsolution.com/api/v1/health` and `https://owwsolution.com/`, and that `/api/v1/docs` answers 404 (production mode). Every action is pinned to a commit SHA (the version in a comment); `.github/dependabot.yml` proposes updates weekly. Repository settings: variables `DEPLOY_ENABLED`, `DEPLOY_HOST`, `DEPLOY_USER`, `VITE_API_BASE_URL`, `VITE_TURNSTILE_SITE_KEY`; secrets `DEPLOY_SSH_KEY`, `DEPLOY_KNOWN_HOSTS` in the `production` environment only (deployment branch policy: `main`), never at repository level, so no pull request, even one that edits the workflow, can read them. The deploy key exists only in that secret and the VM's `authorized_keys`, where it is prefixed `restrict` (commands and scp; no forwarding, no pseudo-terminal). To rotate it (last: 2026-09-25): generate a new pair, add `restrict <new public key>` next to the old one, check a login, set the environment secret, remove the old line, and never keep the private key on disk.
- **DNS** (Namecheap BasicDNS): A records `@`, `www`, `api` → `34.26.232.31`; Caddy obtains the Let's Encrypt certificates on first request once they resolve. Email: Resend's domain records (SPF / DKIM / return path) for `owwsolution.com`, so mail from `noreply@owwsolution.com` is accepted.
- **Cloudflare** (Phase 8): the zone `owwsolution.com` on Cloudflare's free plan; `@`, `www`, `api` proxied, the Resend and mail-forwarding records DNS-only; SSL/TLS Full (strict) (Flexible would loop: Caddy redirects HTTP to HTTPS), Always Use HTTPS, minimum TLS 1.2; Rocket Loader and Email Address Obfuscation off (both rewrite pages with inline scripts the CSP refuses); Web Analytics with automatic setup. Caddy takes the visitor's address from `CF-Connecting-IP` only for requests from Cloudflare's ranges (section 6.3). Since 2026-09-24 the zone is active and the GCP firewall admits HTTP(S) from Cloudflare only: rule `allow-web-cloudflare` (tcp 80, 443, udp 443 from Cloudflare's IPv4 ranges, https://www.cloudflare.com/ips-v4, to tag `web`; the VM has no IPv6) replaced `allow-web`, which is kept disabled (re-enable it only to serve the VM directly, e.g. with Cloudflare switched to DNS only). Nothing reaches the VM's ports 80 / 443 around Cloudflare; SSH stays open for the deploy (key-only). When Cloudflare announces new ranges, update both this rule and Caddy's `trusted_proxies`. Caddy still renews its Let's Encrypt certificates through Cloudflare: the HTTP challenge is redirected to HTTPS at the edge, and Caddy answers ACME challenges on its HTTPS server too (check the renewal in late November, 30 days before 2026-12-24). Caching: pages and API answers pass uncached (`DYNAMIC`); hashed `/assets/*` are cached at the edge (new names on every deploy); Browser Cache TTL must be "Respect Existing Headers" (Cloudflare's default 4 h would override the origin's `no-cache` on `robots.txt`, `theme-init.js`, the share image).
- **Operations** (on the VM, `cd /opt/anthonyzng-web`, as `deploy` or with sudo): `docker compose ps`, `docker compose logs -f backend`; roll back by re-running the deploy job of an older commit (Actions → the run → Re-run jobs), or on the VM by pulling that SHA's images (after `docker login ghcr.io` with a token that can read the packages), tagging them `latest` and `docker compose up -d`; seed an empty database once with `docker compose run --rm backend python -m app.seed`. The admin panel is then the source of truth; after editing content, run `npm run content:sync -- https://api.owwsolution.com` and commit the snapshot.
- Run exactly one uvicorn worker (the rate limiter is in-process) without an access log; both are fixed in the backend image (section 6.3). A Content-Security-Policy (Phase 8) must allow the API origin in `connect-src` and `img-src` (project cover images are served by `GET /files/{id}`).

---

## 8. Installed Claude Code plugins (project scope)

- `superpowers`: planning / TDD / debugging workflows
- `ui-ux-pro-max`: UI/UX design system guidance
- `code-review`: pre-commit review
- `example-skills` (Anthropic): includes `webapp-testing` and `mcp-builder`

---

## 9. Roadmap

| Phase | Scope |
|-------|-------|
| 0 | Repo init, CLAUDE.md, README, `.gitignore`, GitHub remote, GitHub MCP |
| 1 | Frontend scaffold (Vite + React + TS + Tailwind + i18n + theme), design tokens |
| 2 | Home page: parallax hero, scroll-driven section transitions |
| 3 | Experience, Projects, Skills, Contact, CV download (static data first) |
| 4 | FastAPI + PostgreSQL: content API, contact endpoint, admin auth |
| 5 | Admin panel (CRUD for bilingual content) |
| 6 | Dockerize all tiers + Caddy, full-stack local run |
| 7 | GCP VM + DNS + HTTPS, GitHub Actions auto-deploy |
| 8 | SEO, analytics, performance and accessibility pass |

---

## 10. Open Questions (TBD)

- None at the moment. (The localised content storage model was decided in Phase 4: one JSONB `translations` column per row, see section 5.)
